# Changelog

本文记录 DirBridge 的版本变化。用户发布说明应优先摘取“用户可见变化”“修复”“已知问题”，开发者内部信息保留在“开发者说明”。

## v0.7.5 - 2026-08-20

### 用户可见变化

- 文件面板支持多文件、多文件夹及混合选择的拖放和复制粘贴；本地与远程面板之间、Windows 桌面和资源管理器之间可以沿用同一套上传下载流程。
- 远程文件和文件夹可以直接拖到 Windows 资源管理器，并在资源管理器请求内容时通过现有传输任务后台下载。
- 上传和下载遇到同名目标时提供覆盖、跳过、重命名和“应用到全部”；覆盖文件或文件夹时先写入临时目标，成功后再替换原目标。
- 本地和远程文件列表增加 `..` 上级目录项，支持双击、Enter 和同侧拖放移动到上一级目录。
- 文件列表支持 `Ctrl+C`、`Ctrl+V`，右键菜单显示复制、粘贴和重命名对应的 `Ctrl+C`、`Ctrl+V`、`F2` 快捷键提示。
- 目录传输在任务列表中显示父子层级、文件明细、批次进度以及取消和失败结果。

### 修复

- 修复传输或远程剪贴板操作后退出应用时，Windows Shell 数据对象未正确释放而可能导致窗口无法关闭的问题。
- 修复 Windows Shell 拖放生命周期线程不正确时可能出现高 CPU、拖放无法结束或重复错误提示的问题。
- 修复批量传输中目标目录、冲突决策和取消状态未完整传递，导致任务落入错误目录或剩余任务继续执行的问题。
- 修复覆盖传输失败时可能损坏原目标、遗留临时对象或未刷新目录任务明细的问题。

### 开发者说明

- Windows 虚拟文件数据对象实现 `IDataObjectAsyncCapability`，耗时内容提取与 UI/OLE 拖放生命周期分离。
- 文件和目录覆盖复用安全替换流程；批次任务使用统一标识聚合进度、冲突决策和取消传播。
- 自动化覆盖应用内拖放、复制粘贴、Windows 虚拟文件描述与内容流、冲突处理、安全替换和退出保护。

### 已知限制

- 第一阶段不支持断点续传、跨系统剪切或特殊 Shell 目标；取消后重新传输会从头开始。
- Windows 桌面、资源管理器、OneDrive 普通目录和不同权限组合的完整兼容矩阵仍需按实际环境持续验证。
- FTP/SFTP 服务端对替换和回滚能力存在差异；安全覆盖、失败回滚和临时对象清理仍应在目标服务器上验证。

## v0.7.4 - 2026-08-12

### 用户可见变化

- 本地拖拽、远程内部移动以及 FTP/SFTP 上传下载遇到同名文件或文件夹时，统一提示用户重命名后继续或取消；不再覆盖或合并目标内容。
- 文件与文件夹拖拽统一限制为单个项目，暂不支持批量拖拽，减少批量冲突和部分成功造成的不确定状态。
- SSH 终端内容超过可视区域时显示右侧滚动条；滚动到底部时与当前终端输入位置保持一致，输入或粘贴会返回最新输出。
- SFTP 站点可按需开启旧版 `ssh-rsa` 主机密钥兼容；该选项默认关闭，且不启用 `ssh-dss`。
- SSH 主机密钥变化导致连接失败时，会显示服务器提供的新指纹；用户确认设备可信后，可移除该主机的旧记录并自动重新连接。
- 应用日志支持从菜单复制全部内容；文件列表移除“所有者”列，权限菜单仍保留远程所有者信息。

### 修复

- 修复 FTP 目录创建、删除和重命名等命令使用错误目录上下文，导致连续操作或特定服务端执行失败的问题。
- 修复取消文件夹上传后，目录聚合任务长期停留在“取消中”的问题；清理子任务后目录任务也会进入已取消状态。
- 修复不同拖拽、上传和下载入口对同名目标分别执行跳过、覆盖或合并，导致基础操作行为不一致的问题。

### 开发者说明

- FTP 目录命令在目标父目录上下文中使用项目名称执行，SFTP 路径保持不变。
- 重名处理复用统一对话框和名称校验，文件扩展名在重命名建议中保持不变；自动化测试使用确定性的 `-renamed` 名称。
- SSH 兼容配置随站点保存，并通过 Broker 启动协议传递给系统 OpenSSH；只追加 `HostKeyAlgorithms=+ssh-rsa`。
- 主机密钥冲突恢复仍由系统 `known_hosts` 和 `ssh-keygen` 管理；DirBridge 不读取或保存私钥。

### 已知限制

- 主机密钥冲突恢复必须由用户通过可信渠道核对新指纹；无法提取指纹时不会自动删除旧记录。
- 当前禁止批量拖拽；多文件传输仍可通过非拖拽入口按现有传输队列执行。
- 不同 FTP 服务端对目录命令和路径上下文的支持仍可能存在差异，发布前验证结果只代表当前联调环境。

## v0.7.3 - 2026-08-09

### 用户可见变化

- 会话管理器支持删除单条最近会话和清空全部最近会话；站点分组可以独立新建、重命名和删除，空分组也会持久保留。
- 删除非空分组时，站点会移到“未分组”而不会被删除；从分组中新建站点时会自动预填当前分组。
- 本地标签会随当前目录显示实际名称；本地文件树显示状态会在重启后恢复，并与远程站点的文件树状态保持独立。
- 新建文件和文件夹会直接生成默认名称，重名时自动增加序号，并在创建后进入表格内联重命名；`F2` 和右键重命名使用相同交互。
- 本地和远程表格项目可以拖到同级文件夹或文件树目录；本地同盘执行移动、跨盘执行复制，远程项目执行远程移动，并显示对应的目标提示。

### 修复

- 修复 FTP 连续删除文件或目录时可能一次失败、一次成功的问题。
- 修复 FTP 同目录连续重命名文件或文件夹时可能交替失败的问题。
- 修复文件内联重命名输入框与原名称图标、文字位置不一致的问题。
- 修复表格项目拖到文件夹时提示偶发变成禁用，以及远程表格拖放无法进入同级文件夹的问题。
- 完善远程文件操作的常见错误提示；连接超时时不再错误显示“站点已连接”。

### 开发者说明

- 站点分组作为独立配置实体保存，同时兼容旧配置中由站点 `group` 字段形成的分组。
- 本地文件树状态保存到用户设置，远程文件树状态仍按站点配置保存。
- 拖放处理区分进入、移动和释放阶段，并在释放前再次校验目标冲突、自身目录和子目录。
- 本轮功能已完成逐分支人工测试，并在短期集成分支中汇总。

### 已知限制

- 本地文件被外部程序保存后，文件列表不会自动监听并刷新大小；本版本继续保留手动刷新方式。
- 不同 FTP 服务端对命令、路径和权限的支持仍可能存在差异，发布前验证结果只代表当前联调环境。

## v0.7.2 - 2026-08-05

### 用户可见变化

- 远程新建文件、新建目录和重命名改为后台执行，减少操作期间对界面的阻塞。
- 文件树显示状态按保存站点记录；重新启动后再次打开同一站点时，会恢复该站点上次的显示状态。
- 文件面板右键的“文件树”只控制当前面板或当前远程会话，顶部“查看 → 文件树”统一控制本地面板和所有已打开的远程会话。
- 传输列表刷新时保留滚动位置和选中任务，便于持续查看最新任务和取消正在执行的传输。
- 目录上传准备阶段显示状态和经过时间，并在扫描过程中逐步加入传输任务。

### 修复

- 修复远程路径包含空格时，新建、重命名、删除和上传等操作可能失败的问题。
- 修复远程删除成功后仍可能显示失败，以及底层英文错误直接显示给用户的问题。
- 修复关闭外部编辑后残留工作缓存，导致再次编辑提示缓存已存在、保存后不再同步远程文件的问题。
- 修复传输列表定时刷新导致滚动位置回到顶部、任务选中状态丢失的问题。
- 减少远程文件操作中的重复连接、目录探测和列表请求；上传、下载与目录命令分别复用独立连接句柄。

### 开发者说明

- 真实 SFTP 检查已覆盖带空格目录的创建、重复创建错误、空文件创建、重命名、上传、下载、删除和清理。
- UI workflow 覆盖文件树状态持久化、单会话控制和顶部全局控制；核心检查覆盖旧配置默认值与站点配置往返保存。
- MinGW Release 与 MSVC/vcpkg Debug 构建通过，CTest 10 项全部通过。
- 重构 GitHub README，并让发布包携带 README 引用的 `docs/images` 文档图片。

### 已知限制

- 不同服务器、网络和目录规模下的操作耗时仍需在实际使用环境中观察；自动化验证不等同于人工性能基准。
- 传输列表操作手感、外部编辑器组合以及重启后的最终视觉状态仍建议在发布包中进行人工验收。

## v0.7.1 - 2026-08-03

### 用户可见变化

- 远程目录切换改为后台加载；包含大量项目的目录不再阻塞界面，快速连续切换路径时只显示最后一次请求的结果。
- 远程文件和目录右键菜单新增权限修改，可按所有者、组和其他用户设置读写执行权限，并支持递归应用到子目录。
- 本地文件删除改为移入 Windows 回收站，降低误删风险。
- 文件列表“名称”列使用合理的默认宽度，并允许手动拖动表头调整；顶部连接栏增加左侧留白并调整标签间距。

### 修复

- 修复远程目录导航在 UI 主线程串行请求当前目录和祖先目录，导致 SFTP 切换路径明显卡顿的问题。
- 修复远程项目拖拽到本地时丢失文件/目录类型，普通文件被错误按目录递归下载的问题；混合选择文件和目录时也会按各自类型处理。
- 修复目录内容较多时逐行扩展表格和重复获取文件图标带来的额外界面刷新开销。
- 保留连接时已获取的远程目录树信息，避免异步导航后同级目录节点从树中消失。

### 开发者说明

- 远程目录请求增加代次校验，过期结果不会覆盖用户随后选择的路径；会话关闭时等待活动操作结束，避免后台访问已销毁对象。
- libcurl 目录读取使用独立的可复用 easy handle，并通过互斥锁串行保护；上传和下载仍使用各自独立的 handle。
- UI smoke 覆盖大目录填充、单次导航请求、异步响应、混合类型拖拽和权限操作；核心检查补充权限接口覆盖。

### 已知限制

- FTP/FTPS 权限修改依赖服务器对 `SITE CHMOD` 的支持；不支持时会返回服务器错误。
- 递归修改权限不会跟随符号链接；真实服务器的权限策略仍以服务端配置为准。

## v0.7.0 - 2026-08-02

### 用户可见变化

- 新增内嵌 SSH 终端：可从 SFTP 站点树和远程文件标签右键直接打开，不要求对应 SFTP 会话已经连接。
- SSH 终端直接复用站点保存密码，不增加二次确认；每个终端标签使用独立的 Broker、ConPTY 和系统 `ssh.exe`。
- 底部“终端”页支持多个 SSH 标签、ANSI 颜色、光标、滚动历史、选择复制、键盘输入、中文输入法和粘贴。
- 终端支持在右侧中央工作区内最大化，左侧会话管理器保持不变；右键可直接粘贴剪贴板内容。

### 修复

- 修复 Tab 被 Qt 焦点遍历截获，导致远端命令补全失效且终端失去输入焦点的问题。
- 修复中文双宽字符只绘制半个字符的问题。
- 修复 MSVC CTest 未显式提供 Qt Debug 运行库路径，导致终端检查弹出 `Qt6Cored.dll` 缺失错误的问题。

### 开发者说明

- 使用每会话独立 Broker 隔离 ConPTY 生命周期，保留旧 Windows 的运行时能力检测，不提高最低 Windows 版本。
- 终端状态和输入编码由固定版本的 Ghostty VT DLL 提供，并通过适配层与 Qt UI 隔离；DLL、源码和 Zig 工具链不加入 Git。
- 保存密码通过一次性密码租约、认证帧和 AskPass 辅助程序传递，不进入命令行参数或日志。
- 终端输出通过不可变快照合并刷新，关闭标签和应用退出时统一回收 Broker、ConPTY 和 `ssh.exe`。

### 已知限制

- SSH 终端依赖 Windows ConPTY、系统 OpenSSH 客户端以及随程序部署的固定 `ghostty-vt.dll`；缺少运行组件时不影响原有 FTP/SFTP 文件管理。
- Qt 5 + MinGW 和完整低版本 Windows 终端 UI 仍需要最终发布环境复验。

## v0.6.0 - 2026-07-11

### 用户可见变化

- 新增远程文件外部编辑：DirBridge 下载到受控缓存后调用 Windows 默认应用，外部应用保存后自动同步回原远程路径。
- 编辑准备、同步、失败和冲突等信息写入日志；同步成功后会自动刷新当前远程目录，更新文件大小等信息。
- 编辑缓存工作副本保留经过 Windows 文件名规则清理后的远程原文件名；同名文件仍由会话和文档缓存目录隔离。
- 活动编辑文件可关闭监听；最多同时监听 5 个文件，超出时自动停止最久未成功同步文件的监听，连续 5 分钟无成功同步也会停止监听并保留缓存。
- 文件列表选中项目后支持 `F2` 重命名。

### 开发者说明

- 编辑会话使用会话 ID 和规范化远程路径隔离缓存；同名文件和不同远程会话不会共享本地副本。
- 文件和父目录共同监听，连续保存经过防抖后生成稳定上传快照；上传期间再次保存不会被旧上传结果标记为已同步。
- 编辑后台任务持有 `shared_ptr<RemoteFileSystem>`，不依赖 `RemoteSession*` 或 UI 对象裸指针。

### 已知限制

- 外部编辑默认限制为 5 MiB；冲突检测依赖远程大小和修改时间，只能尽力检测。
- 不支持自动合并、多人协作、外部编辑器进程跟踪或跨重启恢复。

## v0.5.10 - 2026-07-11

### 用户可见变化

- 本版本不引入新的最终用户功能，重点修复后台任务与传输队列的安全边界。

### 修复

- 修复目录传输准备完成后追加子任务可能导致父任务指针失效的问题。
- 修复窗口关闭时后台任务仍可能访问已销毁远程后端的生命周期风险。

### 开发者说明

- 后台任务在执行期间持有远程后端所有权；窗口关闭改为先停止新任务、取消可取消传输，再等待活动任务结束后退出。
- `DirBridgeCoreChecks` 已注册到 CTest；默认 CTest 不包含真实 FTP/SFTP 或 UI 自动化测试。

## v0.5.9 - 2026-07-10

### 用户可见变化

- 本版本不引入新的最终用户功能，重点为发布工程整理与代码结构收口。
- 安装包、源码、vcpkg manifest、发布脚本和变更记录已统一到 `0.5.9`。

### 修复

- 收口安装包发布所需的版本面，避免应用版本、安装包版本和发布脚本版本不一致。
- 收口版本号权威来源，补齐 `vcpkg.json` 遗漏，减少后续发版人工漏改风险。
- 补齐仓库换行规则，减少 Windows 环境下 `CRLF/LF` 反复漂移带来的发布噪音。

### 开发者说明

- `MainWindow` 和 `FilePanel` 已按职责拆分为 `window_*` / `panel_*` 模块，便于后续持续维护。
- `src/ui` 以及本轮涉及的 `src/app`、`src/core`、`src/config`、`src/protocol` 中残留英文 Doxygen 注释已中文化。
- 新增 `.gitattributes`，统一源码、文档和 Windows 脚本的换行策略。

## v0.5.8 - 2026-07-08

### 用户可见变化

- 增加 MSVC 构建 preset，便于在 Visual Studio C++ 工具链下配置和构建。
- 增加 vcpkg manifest，用于在 MSVC 环境下从源码构建 `curl[ssh]` 及其依赖。
- 文档补充 MinGW 和 MSVC 的 libcurl ABI 差异，避免 MSVC 误用 MinGW 预编译包。
- 收口 README 与项目文档的职责分工，移除公开仓库中的重复说明和独立图标方案文档。

### 修复

- 修复 MSVC 构建配置可能误指向 MinGW `libcurl.dll.a` 的问题。
- 当 MSVC 下找不到兼容的 `libcurl.lib` 时，CMake 会给出明确提示。

### 开发者说明

- `CMakePresets.json` 新增 `windows-msvc-*` 和 `windows-msvc-vcpkg-*` preset。
- `ThirdParty.cmake` 按编译器选择 libcurl import library：MinGW 使用 `.dll.a`，MSVC 使用 `.lib`。
- `vcpkg.json` 声明 `curl[ssh]`，用于生成 MSVC ABI 的 libcurl 依赖。

### 已知问题

- MSVC 发布包打包流程仍未接入，当前 Release 打包脚本仍以 MinGW preset 为默认。

## v0.5.7 - 2026-07-08

### 用户可见变化

- 明确公开仓库的最低构建环境要求：C++17、64 位 Windows、可用的 `<filesystem>`、Qt Widgets 和 Qt Svg。
- README 增加已验证环境、计划兼容环境和不支持环境说明。
- 修正项目文档中与当前 MVP 能力不一致的过期描述。

### 修复

- 修复 Qt 5 下 `QDropEvent::position()` 不存在导致的拖拽相关编译问题。
- 修复旧版 MinGW / GCC 7.3 构建失败时错误提示不清晰的问题；现在 CMake 配置阶段会明确提示需要支持 C++17 `<filesystem>` 的编译器。

### 开发者说明

- CMake 增加 64 位、GCC 版本、MSVC 版本和 `<filesystem>` 编译能力检查。
- Qt5/Qt6 API 差异采用局部条件编译处理，暂不引入大型兼容层。
- 文档明确当前 `setup_third_party.ps1` 准备的是 MinGW 版 libcurl；MSVC 构建需要匹配 MSVC ABI 的 libcurl 或后续迁移到 vcpkg。
- `docs/00-总体技术方案.md` 不再记录当前实现状态，避免稳定设计文档承担版本状态维护职责。

### 已知问题

- MSVC 已有本地编译和 Core 检查通过记录，但仓库依赖准备脚本和发布打包流程仍以 MinGW 为主。
- Qt 5 兼容策略已保留，但仍需要完整 Qt 5 构建验证。

## v0.5.6 - 2026-07-01

### 用户可见变化

- 站点密码使用 Windows 当前用户 DPAPI 加密后保存，不再以明文写入 `config/sites.json`。
- 旧版明文站点配置仍可读取，并会在下次保存时迁移为加密字段。
- 状态栏启动信息改为面向用户的“就绪”或异常提示，依赖详情保留在日志中。

### 修复

- 修复站点密码明文落盘的问题。
- 修复启动后状态栏显示 `libcurl / JSON / logging` 等内部诊断信息的问题。

### 开发者说明

- 新增 `PasswordCrypto`，Windows 构建使用 DPAPI 当前用户范围保护站点密码。
- `SiteProfile` 保存为 `passwordProtected` 和 `passwordStorage` 字段，仍兼容旧版 `password` 明文字段读取。
- CMake Windows 构建新增 `Crypt32` 链接。

### 已知问题

- 加密后的站点密码绑定当前 Windows 用户和本机环境，复制到其他用户或机器后不能直接解密。

## v0.5.5 - 2026-06-29

### 用户可见变化

- 目录上传和下载显示文件夹父任务，可展开查看子文件传输明细。
- 目录父任务按子文件总大小汇总进度，未知大小时按完成数量显示整体进度。
- 上传和下载文件传输改为后台执行，目录拖拽传输过程中主界面保持可操作。
- 传输表进度列改为进度条，父任务显示总进度，子文件显示细节进度。
- 目录父任务默认折叠，按需展开查看子文件明细。
- 文件列表和传输表按 `B / KB / MB / GB / TB` 自动换算文件大小。
- 传输成功、取消和常见失败消息改为中文展示。
- 文件面板后退、前进、上一级按钮改为仅显示图标。
- 启动时不再显示“远程：未连接”占位 Tab；连接后才创建远程会话 Tab。
- 远程会话底部状态精简为项目数量。
- 无远程会话时本地面板占满文件区；连接远程后本地和远程平分显示。
- 菜单栏收敛为文件、查看和帮助；文件菜单只保留新建站点入口。
- 远程会话 Tab 右键菜单提供重连、断开和关闭会话，重连会复用当前会话 Tab。
- 传输表显示速度、估计剩余时间和经过时间。
- 关于窗口显示项目版本、许可证、GitHub 仓库地址和作者信息，并改为无提示音的信息对话框。
- 本地文件夹右键可上传，远程文件夹右键可下载。

### 修复

- 修复目录传输只能看到分散文件明细、缺少文件夹整体进度的问题。
- 修复传输表消息列直接暴露 `upload succeeded`、`download succeeded` 等英文底层消息的问题。
- 修复关闭最后一个远程会话后自动恢复无效占位面板的问题。
- 修复无远程会话时刷新远程 action 仍可能可点的问题。
- 修复快速保存同一协议/IP/端口/用户时误覆盖已有站点名称和分组的问题；现在保存为站点会进入新建站点弹窗。
- 修复目录父任务作为运行中任务占用传输并发，导致子任务无法继续调度的问题。
- 修复目录拖拽传输前递归枚举和远程目录准备阻塞主界面的问题。
- 修复本地和远程目录删除在 UI 线程递归执行导致界面卡顿的问题。
- 修复本地文件夹右键上传误走单文件上传入口的问题。
- 修复目录父任务处于准备中时取消按钮不可用的问题。

### 开发者说明

- `TransferJob` 增加任务类型、父任务 id、子任务计数和运行时统计字段，用于目录聚合进度与速度/耗时显示。
- `TransferQueue::nextPending()` 只调度文件任务，目录父任务作为 UI 聚合行保留。
- `RemoteFileSystem` 上传/下载接口增加进度回调，`CurlRemoteFileSystem` 通过 libcurl 进度回调上报字节进度。
- UI 传输调度改为 `QThread` 后台执行，队列状态只在 UI 线程更新。
- 单文件上传的远程同名检查、目录上传/下载准备、本地删除和远程递归删除均移出 UI 线程。
- UI smoke test 更新为适配无占位远程 Tab，并覆盖图标按钮、关于 action、进度条、布局和目录父子传输行。
- CMake 项目版本、QApplication 显示版本、发布脚本和 Inno Setup 默认版本同步为 `0.5.5`。

### 已知问题

- 连接取消目前是逻辑取消，不能强制中断已进入阻塞状态的底层 libcurl 调用。
- 远程刷新和路径跳转仍是同步目录加载流程。
- MVP 阶段仍明文保存站点密码，仅用于本地开发验证。

## v0.5.4 - 2026-06-26

### 用户可见变化

- 补充 Apache License 2.0 项目许可声明和第三方许可证说明。
- 提供 Windows 绿色包和 Inno Setup 安装包发布流程。
- 发布包随附 README、CHANGELOG、RELEASE、项目许可和第三方许可证说明。

### 修复

- 修复 Release 构建下 UI workflow smoke test 对异步连接状态判断过早的问题。

### 开发者说明

- 新增 `windows-mingw-release` preset、发布打包脚本和 Inno Setup 脚本模板。
- 发布脚本会生成绿色包，复制许可证说明，并在包内运行最小 smoke 验证。
- CMake 项目版本和 QApplication 显示版本同步为 `0.5.4`。

### 已知问题

- 如果 `ISCC.exe` 不在 `PATH` 或常见安装目录中，需要通过 `scripts/package_release.ps1 -InnoCompiler <路径>` 显式指定。

## v0.5.3 - 2026-06-26

### 用户可见变化

- 远程首次连接改为后台执行，慢连接时主界面不再被同步阻塞。
- 远程面板会显示连接中状态。
- 连接过程中可以点击取消。
- 连接过程中会拦截重复连接，避免连续创建多个未完成远程会话。

### 修复

- 修复远程连接过程中重复点击连接可能造成状态混乱的问题。
- 修复验证程序可能加载 PATH 中其他软件 MinGW 运行库而启动失败的问题。

### 开发者说明

- 新增远程连接后台任务和连接结果回调，将首次 `connect + listDirectory` 从 UI 线程移出。
- 新增连接取消标记；取消后后台结果返回时不会重新标记会话为已连接。
- 将 UI smoke test 从 `src/app/main.cpp` 拆分到 `src/app/UiSmokeTests.h/.cpp`，主入口只保留应用启动和命令行分发。
- CMake 在 Windows 下为 `DirBridge`、`DirBridgeCoreChecks` 和 `DirBridgeRemoteCheck` 复制 libcurl 与 MinGW 运行库。
- CMake 项目版本和 QApplication 显示版本同步为 `0.5.3`。

### 已知问题

- 当前取消连接属于逻辑取消，不能强制中断已进入阻塞状态的底层 libcurl 调用。
- 远程刷新和路径跳转仍是同步目录加载流程，后续可复用同一套异步任务模型。
- 目录传输仍未提供聚合进度显示。

## v0.5.2 - 2026-06-24

### 用户可见变化

- 接入 DirBridge 应用图标和 Windows exe 图标。
- 接入第一批 Fluent SVG 图标资源。
- 主窗口标题简化为 DirBridge。
- 顶部主工具栏移除无实际作用或重复的按钮。
- 远程 Tab 关闭按钮改为灰色 X。
- 连接失败提示改为单次弹窗，并将用户可见信息中文化。
- 主程序切换为 Windows GUI 子系统，双击运行不再附带终端窗口。

### 修复

- 修正 qrc alias，避免资源路径变成重复目录。
- 修复关闭远程会话时可能因延迟销毁面板导致的访问冲突。

### 开发者说明

- 生成并接入多尺寸 PNG 与 Windows ICO。
- 设置 QApplication、MainWindow 和 Windows exe 图标。
- 本版本不处理远程连接过程中的 UI 卡顿问题，该问题进入 v0.5.3。

### 已知问题

- v0.5.2 中远程连接和默认目录加载仍在 UI 线程同步执行。

## v0.5.1 - 2026-06-22

### 用户可见变化

- 整理 GitHub 面向用户和开发者的 README。
- 统一仓库中的 DirBridge 命名和公开文档入口。

### 修复

- 移除公开文档中的本机路径和过时 XFolder 命名。
- 调整 CMake preset 说明，区分公开配置和本机私有配置。

### 开发者说明

- 将 repo 文档保持为公开仓库可读内容，不使用 Obsidian 双链。
- 当时仍保留路线图文件；后续版本记录迁移到本文件。

### 已知问题

- 尚未提供正式 Release 包或安装包。

## v0.5.0 - 2026-06-16

### 用户可见变化

- 新增站点分组显示和分组重命名。
- 新增独立站点属性编辑。
- 新增最近会话记录。
- 远程会话 Tab 支持关闭。
- Session Manager 可同步当前远程 Tab 状态。
- 连接失败和默认目录加载失败时显示更清晰的错误提示。

### 修复

- 连接默认目录失败时不会把远程面板误标记为已连接。
- 多远程会话之间的断开和关闭状态互不串扰。

### 开发者说明

- 引入多远程会话状态模型，每个远程 Tab 持有独立会话 id、站点配置、后端实例、面板和当前路径。
- 用户设置只记录最近会话，不自动恢复远程 Tab，也不自动重连。
- 增强 UI smoke 覆盖 Session Manager、多远程会话和远程工作流。

### 已知问题

- 不做启动自动重连。
- 不做启动恢复远程 Tab。
- MVP 阶段仍使用明文密码策略，仅用于本地开发验证。

## 早期归档

| 版本 | 摘要 |
| --- | --- |
| v0.4.1 | 修复目录传输后发现的文件树和传输显示问题。 |
| v0.4.0 | 添加传输取消、重试、清理已完成任务和默认并发限制入口。 |
| v0.3.0 | 完成本地/远程文件面板、多远程会话、远程文件操作和基础传输队列闭环。 |
