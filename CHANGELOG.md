# Changelog

本文记录 DirBridge 的版本变化。用户发布说明应优先摘取“用户可见变化”“修复”“已知问题”，开发者内部信息保留在“开发者说明”。

## v0.5.3 - 2026-06-26

### 用户可见变化

- 远程首次连接改为后台执行，慢连接时主界面不再被同步阻塞。
- 远程面板会显示连接中状态。
- 连接过程中可以点击取消。
- 连接过程中会拦截重复连接，避免连续创建多个未完成远程会话。

### 修复

- 修复远程连接过程中重复点击连接可能造成状态混乱的问题。
- 修复验证程序可能加载 PATH 中其他软件 MinGW 运行库而启动失败的问题。

### 开发者说明

- 新增远程连接后台任务和连接结果回调，将首次 `connect + listDirectory` 从 UI 线程移出。
- 新增连接取消标记；取消后后台结果返回时不会重新标记会话为已连接。
- 将 UI smoke test 从 `src/app/main.cpp` 拆分到 `src/app/UiSmokeTests.h/.cpp`，主入口只保留应用启动和命令行分发。
- CMake 在 Windows 下为 `DirBridge`、`DirBridgeCoreChecks` 和 `DirBridgeRemoteCheck` 复制 libcurl 与 MinGW 运行库。
- CMake 项目版本和 QApplication 显示版本同步为 `0.5.3`。

### 已知问题

- 当前取消连接属于逻辑取消，不能强制中断已进入阻塞状态的底层 libcurl 调用。
- 远程刷新和路径跳转仍是同步目录加载流程，后续可复用同一套异步任务模型。
- 目录传输仍未提供聚合进度显示。

## v0.5.2 - 2026-06-24

### 用户可见变化

- 接入 DirBridge 应用图标和 Windows exe 图标。
- 接入第一批 Fluent SVG 图标资源。
- 主窗口标题简化为 DirBridge。
- 顶部主工具栏移除无实际作用或重复的按钮。
- 远程 Tab 关闭按钮改为灰色 X。
- 连接失败提示改为单次弹窗，并将用户可见信息中文化。
- 主程序切换为 Windows GUI 子系统，双击运行不再附带终端窗口。

### 修复

- 修正 qrc alias，避免资源路径变成重复目录。
- 修复关闭远程会话时可能因延迟销毁面板导致的访问冲突。

### 开发者说明

- 生成并接入多尺寸 PNG 与 Windows ICO。
- 设置 QApplication、MainWindow 和 Windows exe 图标。
- 本版本不处理远程连接过程中的 UI 卡顿问题，该问题进入 v0.5.3。

### 已知问题

- v0.5.2 中远程连接和默认目录加载仍在 UI 线程同步执行。

## v0.5.1 - 2026-06-22

### 用户可见变化

- 整理 GitHub 面向用户和开发者的 README。
- 统一仓库中的 DirBridge 命名和公开文档入口。

### 修复

- 移除公开文档中的本机路径和过时 XFolder 命名。
- 调整 CMake preset 说明，区分公开配置和本机私有配置。

### 开发者说明

- 将 repo 文档保持为公开仓库可读内容，不使用 Obsidian 双链。
- 当时仍保留路线图文件；后续版本记录迁移到本文件。

### 已知问题

- 尚未提供正式 Release 包或安装包。

## v0.5.0 - 2026-06-16

### 用户可见变化

- 新增站点分组显示和分组重命名。
- 新增独立站点属性编辑。
- 新增最近会话记录。
- 远程会话 Tab 支持关闭。
- Session Manager 可同步当前远程 Tab 状态。
- 连接失败和默认目录加载失败时显示更清晰的错误提示。

### 修复

- 连接默认目录失败时不会把远程面板误标记为已连接。
- 多远程会话之间的断开和关闭状态互不串扰。

### 开发者说明

- 引入多远程会话状态模型，每个远程 Tab 持有独立会话 id、站点配置、后端实例、面板和当前路径。
- 用户设置只记录最近会话，不自动恢复远程 Tab，也不自动重连。
- 增强 UI smoke 覆盖 Session Manager、多远程会话和远程工作流。

### 已知问题

- 不做启动自动重连。
- 不做启动恢复远程 Tab。
- MVP 阶段仍使用明文密码策略，仅用于本地开发验证。

## 早期归档

| 版本 | 摘要 |
| --- | --- |
| v0.4.1 | 修复目录传输后发现的文件树和传输显示问题。 |
| v0.4.0 | 添加传输取消、重试、清理已完成任务和默认并发限制入口。 |
| v0.3.0 | 完成本地/远程文件面板、多远程会话、远程文件操作和基础传输队列闭环。 |
